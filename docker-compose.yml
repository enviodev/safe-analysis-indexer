# Optional local ClickHouse sink for the analytics/ directory.
#
# alpha.23+ auto-launches ClickHouse on `envio dev` when storage.clickhouse: true
# and ENVIO_CLICKHOUSE_HOST is unset. Use this compose file directly only if you
# want a long-running container managed outside `envio dev`:
#
#     docker compose up -d
#
# Then populate the ENVIO_CLICKHOUSE_* env vars (see .env.example).
services:
  clickhouse:
    image: clickhouse/clickhouse-server:25.3-alpine
    container_name: safe-analysis-clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_DB: envio
      CLICKHOUSE_USER: envio
      CLICKHOUSE_PASSWORD: envio
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - clickhouse_logs:/var/log/clickhouse-server
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  clickhouse_data:
  clickhouse_logs:
